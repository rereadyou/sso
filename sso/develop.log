1. linux 下安装 memcached 后需要设置 selinux允许访问端口（11211），否则memcache不能使用。可以查看httpd的error_log确定问题所在。

setsebool -P httpd_can_network_connect true

2. memcach 和 memcached 是两个不同的扩展。
3. memcached 会将php的session名称冠以memc.sess.key.前缀，所以memcached中存储的session id格式为：
memc.sess.key.sessid。
